<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MovieCategoryMapper">
	
	<insert id="insertmoviecategory" parameterType="MovieCategory">
		insert into mp_movie_category(movie_id, category_id, create_at, update_at)
		values(#{movie_id}, #{category_id}, #{create_at}, #{update_at})
	</insert>
	
</mapper>






